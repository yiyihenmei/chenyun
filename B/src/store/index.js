import Vue from 'vue'
import Vuex from 'vuex'


Vue.use(Vuex)

export default new Vuex.Store({
  // 用来存储数据
  state: {
  },
  // 同步修改state数据
  getters: {
  },
  // 异步操作mutations，进而修改state数据
  mutations: {
  },
  // 计算属性
  actions: {
  },
  modules: {
  }
})
